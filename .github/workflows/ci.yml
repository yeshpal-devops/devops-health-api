name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    name: Manual CI Placeholder
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Repository check
        run: echo "CI is intentionally manual for this portfolio repository."
